{
  "name": "portifolio-hugo",
  "private": true,
  "description": "Osmar Petry portfolio — Hugo SSG. npm is only the e2e test runner; the site itself has no JS dependencies.",
  "scripts": {
    "dev": "hugo server",
    "build": "hugo --gc --minify",
    "test:e2e": "playwright test",
    "lint:links": "lychee --offline --no-progress --include-fragments --root-dir \"$PWD/public\" public",
    "check:links": "npm run build && npm run lint:links",
    "build:resume-pdf": "node scripts/build-resume-pdf.mjs",
    "build:all": "npm run build:resume-pdf && npm run build && npm run lint:links"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
